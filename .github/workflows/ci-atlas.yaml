name: Atlas
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - 'sqlite_dir_broken/*'
# Permissions to write comments on the pull request.
permissions:
  contents: read
  pull-requests: write
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: ariga/atlas-action@v0
        with:
          dir: 'sqlite_dir_broken'
          dir-name: 'auth'
          dev-url: 'sqlite://dev?mode=memory'
          cloud-token: ${{ secrets.ATLAS_CLOUD_TOKEN_0SSGS1 }}
  sync:
    needs: lint
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ariga/atlas-sync-action@v0
        with:
          dir: 'sqlite_dir_broken'
          name: 'auth'
          dev-url: 'sqlite://dev?mode=memory'
          cloud-token: ${{ secrets.ATLAS_CLOUD_TOKEN_0SSGS1 }}
