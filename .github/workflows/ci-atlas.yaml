name: Atlas
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/ci-atlas.yaml
      - 'another_work/Directory_employees_MySQL/migrations/*'
  pull_request:
    paths:
      - 'another_work/Directory_employees_MySQL/migrations/*'
# Permissions to write comments on the pull request.
permissions:
  contents: read
  pull-requests: write
jobs:
  atlas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: ariga/setup-atlas@v0
        with:
          cloud-token: ${{ secrets.ATLAS_CLOUD_TOKEN_755RSL }}
      - uses: ariga/atlas-action/migrate/lint@v1
        with:
          dir: 'file://another_work/Directory_employees_MySQL/migrations'
          dir-name: 'ronen-test'
          config: 'file://atlas.hcl'
          env: 'dev'
        env:
          GITHUB_TOKEN: ${{ github.token }}
      - uses: ariga/atlas-action/migrate/push@v1
        if: github.ref == 'refs/heads/main'
        with:
            dir: 'file://another_work/Directory_employees_MySQL/migrations'
            dir-name: 'ronen-test'
            config: 'file://atlas.hcl'
            env: 'dev'
