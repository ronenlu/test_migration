name: Atlas
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/ci-atlas.yaml
      - 'sqlite_dir/*'
  pull_request:
    paths:
      - 'sqlite_dir/*'
# Permissions to write comments on the pull request.
permissions:
  contents: read
  pull-requests: write
jobs:
  atlas:
    services:
      spanner:
        image: gcr.io/cloud-spanner-emulator/emulator
        ports:
          - "9010:9010"
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: ariga/setup-atlas@v0
        with:
          cloud-token: ${{ secrets.ATLAS_CLOUD_TOKEN_YNU3TE }}
      - uses: ariga/atlas-action/migrate/lint@v1
        with:
          dir: 'file://sqlite_dir'
          dir-name: 'sync-action-integration-test'
          dev-url: 'spanner://localhost:9010/projects/project/instances/instance/databases/dev?useplaintext=true;autoConfigEmulator=true'
          config: 'file://atlas.hcl'
          env: 'local'
      - uses: ariga/atlas-action/migrate/push@v1
        if: github.ref == 'refs/heads/main'
        with:
          dir: 'file://sqlite_dir'
          dir-name: 'sync-action-integration-test'
          dev-url: 'spanner://localhost:9010/projects/project/instances/instance/databases/dev?useplaintext=true;autoConfigEmulator=true'
          config: 'file://atlas.hcl'
          env: 'local'